            $(document).ready(function(){
                      $(".skillmap") // select your element (supports CSS selectors)
                .hover(function(){ // trigger the mouseover event
                    $("#placeholder") // select the element to show (can be anywhere)
                        .hide(); // show the element
                }, function(){ // trigger the mouseout event
                    $("#placeholder") // select the same element
                        .show(); // hide it
                });
            });

            $(document).ready(function(){
                      $("#uxdesign") // select your element (supports CSS selectors)
                .hover(function(){ // trigger the mouseover event
                    $("#uxdesigndes") // select the element to show (can be anywhere)
                        .show(); // show the element
                }, function(){ // trigger the mouseout event
                    $("#uxdesigndes") // select the same element
                        .hide(); // hide it
                });
            });

            $(document).ready(function(){
                      $("#infoarch") // select your element (supports CSS selectors)
                .hover(function(){ // trigger the mouseover event
                    $("#infoarchdes") // select the element to show (can be anywhere)
                        .show(); // show the element
                }, function(){ // trigger the mouseout event
                    $("#infoarchdes") // select the same element
                        .hide(); // hide it
                });
            });

            $(document).ready(function(){
                      $("#commdesign") // select your element (supports CSS selectors)
                .hover(function(){ // trigger the mouseover event
                    $("#commdesigndes") // select the element to show (can be anywhere)
                        .show(); // show the element
                }, function(){ // trigger the mouseout event
                    $("#commdesigndes") // select the same element
                        .hide(); // hide it
                });
            });

            $(document).ready(function(){
                      $("#branddesign") // select your element (supports CSS selectors)
                .hover(function(){ // trigger the mouseover event
                    $("#branddesigndes") // select the element to show (can be anywhere)
                        .show(); // show the element
                }, function(){ // trigger the mouseout event
                    $("#branddesigndes") // select the same element
                        .hide(); // hide it
                });
            });

            $(document).ready(function(){
                      $("#graphicdesign") // select your element (supports CSS selectors)
                .hover(function(){ // trigger the mouseover event
                    $("#graphicdesigndes") // select the element to show (can be anywhere)
                        .show(); // show the element
                }, function(){ // trigger the mouseout event
                    $("#graphicdesigndes") // select the same element
                        .hide(); // hide it
                });
            });

            $(document).ready(function(){
                      $("#designstrategy") // select your element (supports CSS selectors)
                .hover(function(){ // trigger the mouseover event
                    $("#designstrategydes") // select the element to show (can be anywhere)
                        .show(); // show the element
                }, function(){ // trigger the mouseout event
                    $("#designstrategydes") // select the same element
                        .hide(); // hide it
                });
            });

            $(document).ready(function(){
                      $("#leadman") // select your element (supports CSS selectors)
                .hover(function(){ // trigger the mouseover event
                    $("#leadmandes") // select the element to show (can be anywhere)
                        .show(); // show the element
                }, function(){ // trigger the mouseout event
                    $("#leadmandes") // select the same element
                        .hide(); // hide it
                });
            });

            $(document).ready(function(){
                      $("#interactiondesign") // select your element (supports CSS selectors)
                .hover(function(){ // trigger the mouseover event
                    $("#interactiondesigndes") // select the element to show (can be anywhere)
                        .show(); // show the element
                }, function(){ // trigger the mouseout event
                    $("#interactiondesigndes") // select the same element
                        .hide(); // hide it
                });
            });


            $(document).ready(function(){
                      $("#userresearch") // select your element (supports CSS selectors)
                .hover(function(){ // trigger the mouseover event
                    $("#userresearchdes") // select the element to show (can be anywhere)
                        .show(); // show the element
                }, function(){ // trigger the mouseout event
                    $("#userresearchdes") // select the same element
                        .hide(); // hide it
                });
            });

            $(document).ready(function(){
                      $("#production") // select your element (supports CSS selectors)
                .hover(function(){ // trigger the mouseover event
                    $("#productiondes") // select the element to show (can be anywhere)
                        .show(); // show the element
                }, function(){ // trigger the mouseout event
                    $("#productiondes") // select the same element
                        .hide(); // hide it
                });
            });

            $(document).ready(function(){
                      $("#productdev") // select your element (supports CSS selectors)
                .hover(function(){ // trigger the mouseover event
                    $("#productdevdes") // select the element to show (can be anywhere)
                        .show(); // show the element
                }, function(){ // trigger the mouseout event
                    $("#productdevdes") // select the same element
                        .hide(); // hide it
                });
            });

            $(document).ready(function(){
                      $("#illustration") // select your element (supports CSS selectors)
                .hover(function(){ // trigger the mouseover event
                    $("#illustrationdes") // select the element to show (can be anywhere)
                        .show(); // show the element
                }, function(){ // trigger the mouseout event
                    $("#illustrationdes") // select the same element
                        .hide(); // hide it
                });
            });

            $(document).ready(function(){
                      $("#story") // select your element (supports CSS selectors)
                .hover(function(){ // trigger the mouseover event
                    $('.backimage').css('background-image', 'url(../img/homepic-wall2.jpg)');
                }, function(){ // trigger the mouseout event
                    $('.backimage').css('background-image', 'url(../img/hero-blue.jpg)');
                });
            });
